{
  "name": "@service-levels/shared",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir dist --declaration --emitDeclarationOnly false",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
